# Extract Source Data Only
# Use this to download project data without running migration

workflow:
  steps:
    - extract-source

source:
  projectKey: my-project
  domain: app.launchdarkly.com  # Optional: Defaults to app.launchdarkly.com

# No destination needed for extract-only workflow

